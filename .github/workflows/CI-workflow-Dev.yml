name: CI_Dev

on:    
  push:
    branches:
      - Dev
    paths-ignore:
      - 'docs/**'

jobs:
  test_matrix_job:
    name: Test matrix job

    env:
      DART_VER: ${{ matrix.dart-version }}
      CODE_VER: ${{ matrix.code-version }}

    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macOS-latest]
        node-version: [10]
        dart-version: [stable, dev]
        code-version: [stable, insiders]

    #if: (github.ref == 'refs/heads/master' && matrix.dart-version == 'dev' && matrix.code-version == 'insiders') || (github.ref != 'refs/heads/master' && matrix.dart-version != 'dev' && matrix.code-version != 'insiders')
    #if: (github.ref == 'refs/heads/master' && DART_VER == 'dev' && CODE_VER == 'insiders') || (github.ref != 'refs/heads/master' && DART_VER != 'dev' && CODE_VER != 'insiders')

    steps:
      - name: Checkout
        uses: actions/checkout@v1
      
      - name: Print environment variables and matrix parameters
        run: |
          echo "github.ref = ${{ github.ref }}"
          echo "matrix.dart-version = ${{ matrix.dart-version }}"
          echo "matrix.code-version = ${{ matrix.code-version }}"
          echo "DART_VER = $DART_VER"
          echo "CODE_VER = $CODE_VER"
