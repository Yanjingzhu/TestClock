name: Test

on:
  push:

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        api_key: [${{ secrets.API_KEY_01 }}, ${{ secrets.API_KEY_02 }}]
    steps:
      - name: Show contexts
        run: |
          echo hello
