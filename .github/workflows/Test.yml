name: Test

on: push

jobs:
  test:
    name: Test
    runs-on: windows-latest
    steps:
      - name: Run with Bash
        shell: bash
        run: |
          echo "**************************************************"
          git --version
          echo "**************************************************"
          echo %PATH%
          echo "**************************************************"

#      - name: Set PATH
#        run: |
#          pathman /as "C:\Program Files\Git\usr\bin\"
#          pathman /rs "C:\Program Files\Git\bin\"
